{
  "engines": { 
    "node": "16.20.1" 
  },
  "name": "puhelinluettelo",
  "version": "0.0.1",
  "description": "Part 3 of Fullstack2021",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd frontend && npm install && npm run build && cp -r build ../",
    "lint": "eslint './**/*.{js,jsx}'"
  },
  "author": "Tuuli Toivanen-Gripentrog",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.1.3",
    "mongoose-unique-validator": "^3.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.3.3",
    "eslint": "^7.11.0",
    "eslint-plugin-react": "^7.32.2",
    "nodemon": "^2.0.15"
  }
}
